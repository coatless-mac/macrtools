% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xcode.R
\name{xcode-toolchain}
\alias{xcode-toolchain}
\alias{developer_dir}
\alias{is_xcode_toolchain_usable}
\alias{xcode_toolchains}
\title{Inspect the Active Xcode Toolchain}
\usage{
developer_dir()

is_xcode_toolchain_usable()

xcode_toolchains()
}
\value{
The active developer directory, or \code{NA_character_} when nothing
usable is selected.

TRUE if a usable compiler is reachable, FALSE otherwise

A data frame with one row per selectable developer directory and
columns \code{path}, \code{type} (\code{"cli"} or \code{"app"}), \code{version} and \code{active}.
}
\description{
Report which developer directory is active, whether a compiler can actually
be reached through it, and which toolchains are available to switch to.
}
\details{
\code{developer_dir()} resolves the active developer directory.
\code{DEVELOPER_DIR} overrides the selection stored by \code{xcode-select}, for
\code{xcrun}, the \verb{/usr/bin} shims and \code{xcodebuild} alike, so it is consulted
first. Note that \code{xcode-select --print-path} echoes its value and exits \code{0}
even when the directory does not exist, so the exit status alone proves
nothing; the path is verified before being returned.

This is the question that matters for building R packages. R is configured
with a bare \code{clang}, which resolves to the \verb{/usr/bin} shim, so whether a
package can be compiled depends on the active developer directory
dispatching successfully, not on where the tools live.

\verb{xcrun --find clang} resolves through the active directory and honors
\code{DEVELOPER_DIR}. It can, however, exit \code{0} while printing an error for a
degraded bundle, so the returned path is confirmed to exist and be
executable rather than trusting the exit status.
}
\section{Listing the Available Toolchains}{


\code{xcode_toolchains()} reports every developer directory \code{xcode-select} could
be pointed at: the Command Line Tools, plus each installed Xcode bundle
wherever it lives. Pass a \code{path} from this table to \code{\link[=xcode_cli_switch]{xcode_cli_switch()}}.

Note that this lists \emph{developer directories}, not \code{.xctoolchain} bundles.
Those are a separate axis selected through \code{xcrun --toolchain} and the
\code{TOOLCHAINS} variable, and are mostly Swift snapshots.
}

\examples{
# Which developer directory is active
developer_dir()
# Check whether a compiler is actually reachable
is_xcode_toolchain_usable()
# List every toolchain xcode-select could use
xcode_toolchains()
}
