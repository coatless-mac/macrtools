# Interface with `xcode-select` Shell Commands

Trigger `xcode-select` commands from within *R*

## Usage

``` r
xcode_select(args)

xcode_select_path()

xcode_select_version()
```

## Arguments

- args:

  Flag arguments to pass to `xcode-select`
