# Check if macOS Sonoma

Sonoma is macOS 14.x, released in late 2023.

## Usage

``` r
is_macos_sonoma()
```

## Value

TRUE if system is macOS Sonoma, FALSE otherwise

## Details

macOS Sonoma (version 14.x) is the successor to macOS Ventura (version
13.x).
