# Running R Version as a Comparable major.minor String

Running R Version as a Comparable major.minor String

## Usage

``` r
r_version_major_minor()
```

## Value

The running R version as a `"major.minor"` string, e.g. `"4.6"`.
