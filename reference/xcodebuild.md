# Interface with `xcodebuild` Shell Commands

Trigger `xcodebuild` commands from within *R*

## Usage

``` r
xcodebuild(args)

xcodebuild_version()
```

## Arguments

- args:

  Flag arguments to pass to `xcodebuild`
