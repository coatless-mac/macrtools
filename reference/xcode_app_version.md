# Version of an Xcode Bundle

Version of an Xcode Bundle

## Usage

``` r
xcode_app_version(bundle)
```

## Arguments

- bundle:

  Path to an `Xcode.app` bundle.

## Value

The bundle's short version string, or `NA_character_`.
